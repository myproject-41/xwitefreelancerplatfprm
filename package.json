{
  "name": "xwite",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter server run migrate",
    "db:seed": "pnpm --filter server run seed",
    "db:studio": "pnpm --filter server run studio",
    "db:generate": "pnpm --filter server run generate"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}